# -*- coding: utf-8 -*-
"""practice_4_Pokormiak.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1N490oBtUt5J02uENx7TltTWJEYjal5MA

# I. Functions. Arguments.

1. (5б) Назвіть основні 4 смислові  частини, з яких складається функція.

функція оголошується як
<code>
def function_name(arguments):
  (code)
</code>
чесно кажучи я не зовсім розумію що мається на увазі у завданні, але мабуть мова йде про слово для оголошення функції (def), її назва (function_name), аргументи, які вона приймає (arguments) і кусочок коду, який виконує функція. ну і потім її можна викликати зі своїми аргументами

2. (3б) Визначте функцію, що приймає 2 аргументи та повертає результат ділення 1го аргумента на інший.
"""

def division(a, b):
  return a/b
division (256, 16) #завжди повертає float

"""3. (2б) Викличте вище визначену функцію, де другий аргумент дорівнює 0."""

print (division (9, 0)) #буде видавати помилку ZeroDivisionError, поки ми не запустимо код з 4 завдання

"""4. (4б) Перевизначте функцію так, щоби у випадку ділення на 0 функція повертала None, а в решті випадків - результат розрахунків."""

def division(a, b):
  if b == 0:
    return None
  return a/b
print (division (9, 0))

"""5. (5б) Визначте функцію, що віднімає одне число від іншого (підказка: це буде 2 аргументи функції). При чому, якщо не визначити, скільки саме має бути віднято від числа, за замовчуванням має відніматися 1. Викличте функцію, передаючи тільки один аргумент."""

def subtract(a, b = None):
  if b == None:
    return a-1
  return a-b
subtract(19)

"""6. (7б) Визначте функцію, яка приймає iterable (ітерабельний обʼєкт, напр. список), що складається з 7ми рядкових значень - назв погодних умов (напр. "sunny") за кожен день у тижні. Повертає функція словник, де ключами є рядки з погодними умовами, а значеннями - кількість разів, скільки дана погода була протягом тижня (напр. {'sunny': 1, ...}). Викличте цю функцію та виведіть результат її роботи на екран."""

def day_count(list_el):
  day_dict = {}
  for i in list_el:
    if i in day_dict:
      day_dict[i] += 1
    else:
      day_dict[i] = 1
  return day_dict
weather_list = ("sunny", "rainy", "cloudy", "rainy", "foggy", "rainy", "cloudy")
print (day_count(weather_list))

"""7. (15б) Напишіть програму, яка б шукала необхідне число у впорядкованому списку цілочислених значень, де
число має вводитись з консолі.
власне алгоритм пошуку винесений в окрему функцію, яка повертає булеве значення (знайдено число - True, не знайдено - False).
в залежності від результату функції, виведіть на екран повідомлення про успішний або ж неуспішний пошук.
Зауваження: використовувати оператор in заборонено.
"""

def search(input, el_list):
  for i in range (len(el_list)):
    if input == el_list[i]:
      return True
  return False
elements = [1939, 16, 27, 14, 19, 2001]
not_searched = True
while (not_searched):
  print ("введіть число для пошуку")
  entered = input()
  if entered.isdigit():
    user_input = int(entered)
    not_searched = False
    print(search (user_input, elements))
  else:
    print("не числове значення, введіть ще раз")

"""# II. Lambda functions.

8. (4б) В яких випадках варто використовувати лямбда-функції?

лямбда-функції можуть приймати кілька значень, але мають лише один вираз. їх доцільно використовувати коли потрібна ненадовго анонімна функція, тобто вона не має імені і завжди повертає значення. її часто вкладають в оголошену функцію для обчислень. а також вона юзається в уже закладених функціях map() або filter()

9. (9б) Визначте список з рядками. Після цього визначте анонімну функцію для отримання першого символу у верхньому регістрі для кожного рядка списку.
Підказка: використовуйте анонімну функцію в якості агрумента до вбудованої ф-ції, яка створена для застосування функцій над iterable обʼєктами. Результат виведіть на екран у вигляді кортежу.
"""

str_list = ['surasiu', 'nebepaleidžia', 'būsi', 'tyla', 'paleisiu', 'malda', 'kada', 'rasa', 'des']
tup_letters = tuple(map(lambda x: x[0].upper(), str_list))
print (tup_letters)

"""10. (10б) Визначте список з числовими значеннями (і цілими, і з рухомою комою). а) Відфільтруйте всі числа, які діляться націло на 3 за допомогою вбудованої функції та анонімної функції в якості аргумента). Збережіть результат у змінній, не приводяти явно до будь-якого типу. б) Використовуючи результат на кроці 12.а у якості аргументу, та за допомогою лямбда-функції в якості іншого аргументу до вбудованої функції, додайте до кожного елементу число 1 та опісля зведіть у квадрат. Остаточний результат приведіть явно до списку та виведіть на екран."""

numbers = [19, 201, 19.39, 271, 6.66, 2.14, 66]
filtered = filter(lambda x: x % 3 == 0, numbers)
result = list(map(lambda x: (x + 1) ** 2, filtered))
print(result)

"""# III. Decorators.

11. (11б) Визначте декоратор, який би виводив у консоль повідомлення про початок роботи і назву функції (напр. my_function._ _ name _ _), а після відпрацювання функції - повідомлення про закінчення роботи і назву функції, що задекорована.
"""

def deco(func):
  def wrapper(a, b):
    print("Start of work:", func.__name__)
    result = func(a, b)
    print("End of work:", func.__name__)
    return result
  return wrapper
@deco
def add(a,b):
  return a+b
print(add(19, 8))

"""12. (4б) Задекоруйте двома різними способами функції з завдання 4 та 5 декоратором із завдання 11. (**УВАГА**: не декоруйте функції двічі!)."""

def deco(func):
  def wrapper(a, b=None):
    print("Start of work:", func.__name__)
    result = func(a, b)
    print("End of work:", func.__name__)
    '''
    це ж мається на увазі як "декоратор"
    з завдання 11?
    '''
    return result
  return wrapper
@deco
def division(a,b):
  if b == 0:
    return None
  return a/b
def subtract(a, b = None):
  if b == None:
    return a-1
  return a-b
subtract = deco(subtract)

"""13. (2б) Викличте одну з задекорованих функцій і виведіть її результат на екран."""

print(division(39, 13))

"""# IV. Recursion.

14. (4б) Назвіть дві частини функції, які є обовʼязковими для того, щоб називати її рекурсивною.

для того, щоб функція була рекурсивною, вона має звертатись до самої себе, а також мати якусь базову умову, до якої функція, викликаючи саму себе, рано чи пізно дійде

15. (15б) Визначте функцію, яка рекурсивно реалізовує пошук числа у послідовності Фібоначчі за її індексом (нумерація починається з нуля). Наприклад, потрібно знайти перше число у послідовності, тоді функція прийматиме 0 у якості аргумента і повертатиме 1, якщо ж знаходити третє число (індекс 2) послідовності, то функція повертатиме 2, якщо ж знаходити десяте число (індекс 9), то функція повертатиме 55.
"""

def fib(n):
  if n <= 1:
    return 1
  return fib(n-1) + fib(n-2)
print (fib(9))

"""# Вітаю! Ви велика(ий) молодець, що впоралась(вся). Похваліть себе та побалуйте чимось приємним. Я Вами пишаюся."""